{{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python3 init_db.py && python3 seed_from_csv.py --csv products_template.csv --clear"
  },
  "deploy": {
    "startCommand": "python3 bot.py"
  }
}
